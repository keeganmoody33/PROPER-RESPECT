# Explicit Claude selected-file CLI — 2026-09-24

Source-only implementation. Baseline: 20b91787feca9f99ef81a8be7c9a6fd06147c99e.
Ref: https://plan.ref.tools/KetN0yyIT4buIGrz
Evidence: /tmp/proper-respect-claude-selected-file-cli-20260924/.

## Architect phases

1. Ground — in progress: trace CLI, sanitizer, report and private projection.
2. Sketch — pending: independently compare explicit mode interfaces.
3. Agree — standing implementation authorization; no extra checkpoint requested.
4. Implement — pending.
5. Scrap — conditional on repeated shape failures; none established.

## Arena phases

1. Frame — explicit exclusive mode; legacy compatibility; strict sanitizer reuse; truthful operator obligations; real generated-fixture CLI proof.
2. Fan out — pending.
3. Cross-judge — pending.
4. Pick — pending.
5. Graft — pending.
6. Verify — pending.

No genuine data, native process, configuration inspection, telemetry, receiver,
backend synchronization, authenticated ingestion, recurrence, publication or deployment.
Owner-supplied labels in tests describe software behavior, never genuine evidence.

## Grounded shape and synthesis

The CLI is the only missing boundary: the strict sanitizer already accepts both
sample designations. Native evidence hashes origin into each fact; changed origin
quarantines a stream. Report and private projection preserve unverified origin,
exact strings and independent nanoseconds. No domain/schema changes are needed.

Three inherited-model architect candidates were read in full. A/C propose a
leading two-choice switch; B proposes a named-value selector plus legacy alias.
The independent cross-judge chose A and C's proof matrix. Parent agrees: A/C pass
all six rubric axes; B adds redundant alias grammar and incorrectly assumes the
old option parser rejects selectors after positional files. Reject B's extra
syntax, graft C's emitted-byte downstream tests and report/preview conflict gate.
No new parser module, forwarding layer or output writer is warranted.

Usage: preserve leading --synthetic; add leading --owner-supplied. Derive one
local sample value after validating exactly one leading selector; reserve both
selector tokens throughout remaining argv before any read. Pass sample directly
into sanitizeClaudeNativeMetrics. Existing options and stdout remain unchanged.

Tradeoffs: one origin per invocation; first-position mode; owner-supplied means
unverified declaration; file ownership, raw modes, ancestor security and exclusive
stdout capture remain documented operator preflights. No implicit mode, path-based
provenance, post-sanitation relabel or mixed-metric filter.

RED: seven generated-fixture CLI tests, four failing before patch. New-mode
success rejected; a flag-named positional file bypassed late-mode rejection.
Implementation changes only selector parsing, sanitizer input and redacted usage.

Phase update: Ground, Sketch, Agree complete. Arena Frame/Fan out/Cross-judge/Pick/
Graft complete. Implement/Verify in progress. Scrap not triggered. Independent
reviews use inherited models, not cross-provider validation.

## Implementation verification

Generated-fixture CLI suite7/7 and focused68/68 pass. Full npm test1287 pass,
2 existing skips, plus7 Node script tests pass. Lint, final typecheck and synthetic
CI-configuration production build pass. Precommit independent review found no
issues and reran7/7. Independent retained rehearsal verifies both modes, exact
values/times, replay0/conflict2/rejection1, unchanged downstream bytes, branded
preview and4 intercepted-local-file Chromium viewport/theme cases. No listener.
Parent recomputed52 artifact and7 exercised-source hashes successfully.

Implement complete; Arena Verify complete for working bytes. Scrap unnecessary.
Committed-head review, current hosted CI/comments and coordinated normal merge
remain external gates tracked in the dated receipt; this file does not claim them.
