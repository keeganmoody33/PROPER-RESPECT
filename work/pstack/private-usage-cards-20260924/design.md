# Exact private usage cards — 2026-09-24

## Usage and shape

`projectPrivateUsage(buildUsageCostReport(texts))` produces a versioned, allowlisted `private-usage-preview-v1`, containing separate `private-usage-card-v1` attachments. `ProductCard` accepts an attachment only for owner display of its exact product; existing `card.activity`, `card.cost` and all public/Convex schemas stay unchanged. A local development script renders the actual component and stylesheet into a standalone directory. No application route or storage path is added.

## Synthesis decision

Three independent inherited-model candidates considered an integrated private attachment, browser File-input wrapper, and gated Next page. Read-only judge scored A 25/25, B 21/25, C 20/25. Parent agrees with A: the smallest caller hides projection/identity removal and preserves actual card interaction without application providers. This is same-model independent review, not cross-provider validation.

Grafts from B: retain sourceVersion, use fixed safe reasons, test actual publication rejection alongside valid ordinary activity. From C: unmistakable local/not-saved/synthetic context. Source observations are required, independently labelled from reconciled coverage; no sums. Browser file input and development route are deferred because neither is necessary for the local generated preview.

Exact count and money strings never use numeric formatting. Null remains unknown, zero remains zero. Codex account snapshots remain origin-unverified and unpriced; the synthetic preview context is separately labelled. Caller-declared owner-supplied evidence is never described as validated genuine metering. Baselines, conflicts, resets, missing categories and gaps retain source semantics. Synthetic valuation remains opt-in in the existing report builder.

## Authority

The owner audience is a presentation guard, not authentication. A local output artifact must not be hosted. No persistent/private read API is introduced; existing two-user storage behavior remains unchanged. Public activity schemas do not admit this projection; forced visitor rendering suppresses it. Existing publication and WebMCP behavior remain tested.

## Verification

RED then behavior tests for mapping/rendering; publication rejection and compatibility; generated artifact desktop/mobile light/dark screenshots, exact text, disclosure keyboard behavior, local brand assets and network exclusion. Independent exact-head review, hosted checks and all review dispositions precede merge. No live collector, account invocation, production update or deployment.
