# Future / Parked Decisions

These documents contain useful exploration, but they are not active MVP scope.

A parked decision is not deleted and not accepted for immediate build. It can be revisited after the core product-stack profile works.

Current parked themes:

- B2B company dashboards
- Company reward configuration
- Pricing and paid tiers
- Public API monetization
- Analytics dashboards
- Screen-time/mobile usage tracking
- Notifications, embeds, gestures, trend metrics
- Support/churn operations

Do not use these documents to justify MVP scope unless they are explicitly moved back into `docs/adr/` and reconciled with `CONTEXT.md`.
