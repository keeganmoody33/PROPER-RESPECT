# Profile production release

Verified 2026-09-19, America/New_York. Owner explicitly approved the previously
prepared production release. This receipt supersedes the earlier hosted source
status, not its historical verification record.

## Source and targets

- Exact application source: `a2539b91e84cd93271daa71550430e32fc95e24e`.
- Vercel project: `groundskeep/proper-respect`, `prj_yxsUnnPW0ka8mkFr7l66eUSzJgT8`.
- READY deployment: `dpl_yek1HfE7u8QNa4E1cfWoCiopCnRU`.
- Deployment URL: https://proper-respect-phg5n37c7-groundskeep.vercel.app.
- Production backend: `striped-chicken-693`.
- Public address: https://proper-respect.com/keegan, redirecting to the existing
  Clerk-compatible host at https://props.lecturesfrom.com/keegan.
- Collection: https://proper-respect.com/onboarding.

The release came from `git archive` of the exact approved commit. Local secrets,
private operator receipts and Git history were excluded. No Git push occurred.
Deployment metadata records `releaseSourceSha`. The accepted checkout differs
from the application checkpoint only in documentation.

## Target and build verification

Read-only Vercel inspection verified the project ID and prior READY deployment.
Fresh production configuration confirmed the expected Convex URL and Clerk live
publishable key. Vercel retained its secret values; no credentials were replaced.
Backend dry run identified the exact production deployment and deleted no indexes.

The backend was synchronized first using a pinned production environment file,
with codegen/typecheck disabled to preserve reviewed local source. Root build
and focused source checks had already passed. Vercel then ran the unchanged
deployment preflight, production Next.js build and TypeScript check, followed by
its existing Convex deployment step against the same verified target. All passed.

The remote function specification exposes 77 functions, including the four
discovery-review operations. Both publication preview hash and revision are
required. No collectors were enabled: the production snapshot has no mailbox
accounts, metric subscriptions or product sources.

## Hosted checks

- Existing signed-in Clerk session reloads the private collection and review
  controls without browser errors. This is not a fresh sign-in proof.
- GitHub uses the existing owner-selected account destination. Four retained
  GitHub records appear under one product selector without merging their data.
- Copilot's official white lockup loads, and its card uses the scoped Mona Sans
  family. It remains an unconfirmed private discovery.
- Anonymous HTTPS retrieval of `/keegan` returns 200. The canonical domain
  redirects correctly. Unknown routes and disabled evidence fixtures return 404.
- Public desktop layout was inspected at 1280 CSS pixels. Mobile at 390 pixels
  had a 375-pixel document and 343-pixel cards, with no horizontal overflow.
  Expanded GitHub details have no internal vertical scroller. The viewport
  override was reset. Public-page browser error logs were empty.
- The public page shows the four existing published cards and the saved name/bio.
  Private source-record text is absent. No sharing selections were changed.
- An unauthenticated discovery-review request returned no data and a generic
  production server error. Its specific rejection reason could not be confirmed
  in the bounded log capture; do not count it as a traced authentication verdict.
  Development rejection and focused authorization regressions passed earlier.

Private before/after exports compared all 32 application tables. Every record
was unchanged after backend synchronization and again after frontend deployment
and browser verification. This includes original evidence, owner decisions,
credentials, cursors and the published snapshot. System metadata is excluded.

## Rollback and limits

Previous READY frontend: `dpl_DD4jWfKFtrCKYAPxvK8ufntusCW6`, application
`d2266faff48bf764e56e93a8536d0fba9ab3783b`. A frontend rollback must preserve the
compatible additive backend receipt fields, FILE_UPLOAD support, mailbox
ambiguity field and publication guards. Do not restore data or rotate keys.

No data transfer, usage-provider read, relationship save, handle restoration,
recurring collection or publication occurred. Existing presentation metadata
was reused; retained brand tables were unchanged.

NotebookLM and Devin Desktop still use text fallbacks. The preparation status
counts Context.dev eligibility separately from the verified Copilot static asset.
Production lacks the richer development Wispr/GitHub originals and saved choices.
New activity selection/persistence, real discovery attachment and fresh sign-in
remain outside this release's live proof. The owner selected `/keegan`; guarded
account/site restoration and any private transfer require their own authorization.
The final owner-approved public profile is not yet complete.
