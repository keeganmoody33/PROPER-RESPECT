# Retained discovery release and live proof

Verified September 19, 2026, 21:56 EDT. The owner approved the runtime correction
and the previously scoped retained-only pass. Both operations completed.

## Exact release

- Source: `32aa043c48ee32686b3c656127f2d58ed7d1388b`.
- Vercel project: `groundskeep/proper-respect`, `prj_yxsUnnPW0ka8mkFr7l66eUSzJgT8`.
- READY deployment: `dpl_8zGq3Exu68QgVvCKmdy4Hm9SoRTh`.
- Deployment URL: https://proper-respect-ivkesju5q-groundskeep.vercel.app.
- Collection: https://props.lecturesfrom.com/onboarding.
- Convex production: `striped-chicken-693`.
- Rollback: `dpl_AJeXca75XZNAhM3NYuhRfYRDcdLX`, source `5a0b77f`.

The seven-file runtime diff hash matched the prepared candidate. All 325 tracked
archive files matched the exact approved Git source before and after backend
synchronization. The archive contained no local environment or Git history.
The later GitHub native-session fix at `82faddf` was not deployed.

Both targets were verified before mutation. A fresh private production backup
included file storage. Backend synchronization completed first, without deleting
indexes. The production function specification exposes `recheckRetained`.
Vercel then passed configuration checks, Next compilation, TypeScript, and its
existing second synchronization to the same backend. All 33 application tables
were identical before and after deployment, prior to the retained recheck.

## One authorized retained-only pass

The signed-in owner used the deployed application control through browser
automation. One click processed 969 pending retained headers, below the approved
1,000-header cap, and reached its final page.

- 118 headers matched the expanded verified catalog.
- 20 new private product candidates were retained.
- 851 headers remain unmatched. They are not 851 products.
- Zero headers were already classified or skipped in this pass.
- No new provider usage or Gmail requests were made by this path.

Snapshot comparison found only additions: 20 products, draft imports, private
candidate props, canonical links, brand jobs and brand snapshots, plus 118 proof
links. All original rows are unchanged. This includes original captures and
provenance, saved relationships, relationship history, sources and freshness,
mailbox accounts, encrypted credentials, query/cursor state, runs and publication.

All new drafts are PENDING and owner-bound. Their candidate props are DRAFT with
no confirmation, go-to designation or activity. Every supporting header belongs
to that owner and its original source/account/message identity. All 118 headers
are distinct; all proof pairs are unique. Their activity actors remain UNKNOWN
and their observations remain empty. All 20 new brand jobs are READY and have
retained snapshots. Brand retrieval adds no usage evidence.

Only one production pass was authorized and run. Replay behavior remains covered
by the previously passing local tests and retained-data replay, not a second
production invocation. The original bounded Gmail runs remain terminal and
recurrence remains disabled.

## Hosted acceptance

The existing authenticated collection loads. All 20 new candidates render in
Discoveries and survive reload. The collection initially loads 25 records;
the existing Load more products control exposes the remaining records. The
owner's earlier saved Testing choices still render after reload.

The Cursor candidate opens supporting details that explicitly say the
relationship is not confirmed. Normal cards omit brand-retrieval diagnostics.
Loaded logos and retained brand styles render. Desktop and narrow-screen checks
used actual measured viewport widths of 1066px and 325px; document widths were
1054px and 312px. The temporary viewport override was reset. This does not claim
every vendor font or logo contrast has received a complete design audit.

The sharing preview has no selected cards and no public products at the current
handle. Publish remains disabled. Anonymous curl returns 200 for the preserved
`/keegan` publication and 404 for unpublished `/lecturesfrom`. A Python urllib
request was rejected with edge error 1010; the normal curl request succeeded.
The published database snapshot is unchanged.

No provider read, development-data transfer, relationship choice, recurrence,
publication or Git push occurred. Backups and detailed account checks remain
outside Git. Fresh sign-in was not exercised. Historical coverage remains partial.
The next owner action is to review the newly recognized products and save only
the relationships that are true, then select and approve an exact sharing preview.
