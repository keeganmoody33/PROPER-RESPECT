# Created by `npx convex dev`; validated before a public profile is queried.
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Browser-visible origin used for canonical profile and Open Graph URLs.
# Production must set this to https://props.lecturesfrom.com.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
