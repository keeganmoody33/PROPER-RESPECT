# Next.js / Vercel runtime
# Use the URL from the same Convex deployment that receives the backend deploy.
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Verified public origin for canonical URLs and share images; separate from OAuth.
# Keep the currently serving host until the separately approved domain cutover.
PUBLIC_SITE_ORIGIN=https://props.lecturesfrom.com

# Clerk application keys. Use matching test keys locally and live keys in production.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
CLERK_SECRET_KEY=sk_test_replace_me

# Clerk routes used by middleware and the onboarding flow.
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/onboarding
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/onboarding

# Convex deployment only — do not expose these to the browser or Vercel client.
# Set CLERK_FRONTEND_API_URL to the issuer/domain shown by Clerk's Convex
# integration. The legacy CLERK_JWT_ISSUER_DOMAIN name remains supported.
# CLERK_FRONTEND_API_URL=https://your-clerk-domain.clerk.accounts.dev
#
# Generate once with: openssl rand -base64 32
# Rotating this key requires a credential migration; existing connector tokens
# cannot be decrypted with a replacement key.
# CONNECTOR_ENCRYPTION_KEY=replace_me_with_a_generated_secret

# Connector credential ownership
# - GitHub OAuth is provided by the user's linked Clerk GitHub account.
# - Devin organization tokens are entered per account and encrypted in Convex.
# There are no shared GITHUB_TOKEN or DEVIN_API_KEY deployment variables.

# Gmail discovery (added 2026-09-17). Configure the same fixed origin in Next.js
# AND Convex. Client secret and encryption keys belong ONLY in Convex runtime.
# MAILBOX_APPLICATION_ORIGIN=http://localhost:3000
# MAILBOX_ALLOW_LOOPBACK_HTTP=true
# Convex runtime only:
# MAILBOX_GOOGLE_CLIENT_ID=your_google_web_client_id
# MAILBOX_GOOGLE_CLIENT_SECRET=your_google_web_client_secret
# MAILBOX_ENCRYPTION_ACTIVE_VERSION=v1
# MAILBOX_ENCRYPTION_KEYS={"v1":"base64_encoded_32_byte_key"}

# Context.dev product presentation only (added 2026-09-17).
# Server-only: Convex for queued enrichment; ignored .env.local for the local
# operator retrieval script. Never prefix this secret with NEXT_PUBLIC_.
# CONTEXT_DEV_API_KEY=replace_me
# Optional local development preview reads retained receipts without API calls.
# PROPER_RESPECT_BRAND_RECEIPTS_DIR=/absolute/private/brand-receipts-directory
